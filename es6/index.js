const letConst = require('./let-const');
const templateLiteral = require('./template-literals');
const dataStructures = require('./data-structures');
const generators = require('./generators');
const proxy = require('./proxy');

module.exports = {
	letConst,
	dataStructures,
	templateLiteral,
	generators,
	proxy
};
